[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "gerrit"
version = "0.1.0"
requires-python = ">= 3.10"
dependencies = ["click"]

[project.scripts]
gerrit = "gerrit:cli"